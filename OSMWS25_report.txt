1. Title Page
	•	Project title: ObjectSegmentationMarsWS25
	•	Course / context: Machine Learning — AAU Klagenfurt
	•	Team members + roles:
	•	[Name] — [Role: data / modeling / evaluation / deployment / PM / writing]
	•	[Name] — [Role]
	•	Date: [ … ]
	•	Repo / demo link (if any): https://github.com/timuriz/ObjectSegmentationMarsWS25
⸻

2. Executive Summary (8–12 lines)
	•	Goal: Segment different classes on Mars surface (semantic segmentation: per-pixel class labeling).
	•	Data: Images + corresponding mask images loaded from Google Drive paths:
	•	images_dir = "/content/drive/MyDrive/ML/images_last_clean_dataset"
	•	masks_dir  = "/content/drive/MyDrive/ML/masks_dataset_last_clean_dataset"
Dataset size in the notebook is printed via len(base_dataset) but the numeric value is not included in the notebook file.
For training/validation the notebook uses a subset n = 200 images (comment says “Limiting to 100 images”, but code sets 200).
	•	Approach: Fine-tune a pretrained DeepLabV3 (ResNet-50 backbone) from torchvision, replacing the classifier head to output 4 classes.
	•	Best result: [Key metric + value] (not logged in the notebook; only losses are printed per epoch).
	•	Main takeaway: Pretrained DeepLabV3-ResNet50 + augmentation + LR scheduling improves robustness/convergence; final quantitative segmentation metrics are not recorded in this notebook.
	•	Next steps:
	•	Add proper segmentation metrics (mIoU / per-class IoU / Dice) on validation/test and report them.
	•	Confirm label semantics (class-id mapping and whether an ignore label exists) and handle it consistently in loss/metrics.

⸻

3. Problem Definition

3.1 Background & Motivation
	•	[Real-world context, why this is useful]

3.2 Task Formulation
	•	Type: Semantic segmentation
	•	Input (X): RGB Mars surface images (loaded via Image.open(...).convert("RGB"))
	•	Output (y): Segmentation mask images (loaded via Image.open(...).convert("L"), i.e., single channel with class indices)
	•	Unit of prediction: Per pixel


3.3 Success Criteria
	•	Primary metric: [F1 / AUC / RMSE / mIoU / …]
	•	Secondary metrics: [precision/recall, latency, memory…]
	•	Minimum acceptable performance: [threshold]

3.4 Constraints & Assumptions
	•	Compute / environment: notebook is structured like Google Colab (paths under /content/drive/...).
	•	Class count assumption: model head is configured for 4 output classes (Conv2d(..., out_channels=4)), implying mask values are expected to be class indices in {0,1,2,3} (not explicitly verified in the notebook).
	•	Ignore label: not implemented in the notebook (nn.CrossEntropyLoss() has no ignore_index), so if you have an ignore pixel in masks, it is currently treated as a normal class unless you change it.

⸻

4. Data

4.1 Data Source & Collection
	•	Source: [Kaggle / company DB / scraped / sensor…]
	•	Time range: [ … ]
	•	Licenses / privacy: [ … ]
	•	Size:
	•	Full dataset size is len(base_dataset) (printed but value not included in .ipynb).
	•	explicitly uses n = 200 samples for splitting/train-val experiments.

4.2 Dataset Structure
	•	Feature groups:
	•	Numeric: [ … ]
	•	Categorical: [ … ]
	•	Text/image: RGB images + grayscale masks (same filename used for image and mask).

4.3 Data Quality & Issues
	•	Missing values: [rate + handling]
	•	Outliers/noise: [what you observed]
	•	Label quality: [uncertain labels? “ignore” class?]
	•	

4.4 Train/Val/Test Split
	•	Split method: manual index split on the first n images.
	•	Ratios: 80/20 train/val using n_train = int(0.8 * n).
	•	train_indices = indices[:n_train]
	•	val_indices   = indices[n_train:]
	•	Test set: not defined in the notebook.
	•	Leakage controls: [what you did to prevent it]

⸻

5. Exploratory Data Analysis (EDA)
	•	Class distribution: [imbalanced?]
	•	Key correlations/patterns: [1–3 findings]
	•	Data examples: [typical + edge cases]
	•	For images: sample images + masks, failure-prone scenarios
(EDA is not present in the notebook beyond visualizing samples.)

⸻

6. Preprocessing & Feature Engineering

6.1 Preprocessing Steps

Implemented via Albumentations:

Image resize: A.Resize(IMG_SIZE, IMG_SIZE) with IMG_SIZE = 768
Normalization: A.Normalize(mean=(0.5,0.5,0.5), std=(0.5,0.5,0.5))
Tensor conversion: ToTensorV2()

Train-time augmentations (in the “reintroduced augmentation” transform):
	•	A.HorizontalFlip(p=0.5)
	•	A.VerticalFlip(p=0.5)
	•	A.RandomRotate90(p=0.5)
	•	A.Affine(scale=(0.9,1.1), translate_percent=(0.0625,0.0625), rotate=(-45,45), p=0.5)
	•	A.RandomBrightnessContrast(p=0.5)

Validation transform uses resize + normalize + tensor conversion (no augmentation).

6.2 Feature Engineering (if applicable)
	•	Created features: [list]
	•	Rationale: [why they help]

6.3 Handling Imbalance / Special Labels
	•	Class weights / sampling / focal loss: [what you used]
	•	“Ignore” labels (if any): not handled in current notebook (no ignore_index in loss; no metric masking).

⸻

7. Modeling Approach

7.1 Baseline
	•	Baseline model: [majority class / linear model / simple CNN…]
	•	Baseline performance: [metric table]
(Baseline is not implemented in the notebook.)

7.2 Candidate Models
	•	Architecture / algorithm: DeepLabV3 with ResNet-50 backbone (torchvision.models.segmentation.deeplabv3_resnet50)
	•	Why chosen: [1–2 reasons]
	•	Key hyperparameters: output channels set to 4 classes by replacing the final classifier layer:
	•	model.classifier[4] = nn.Conv2d(256, 4, kernel_size=1)

7.3 Training Setup
	•	Loss function: Cross-entropy (nn.CrossEntropyLoss())
	•	Optimizer + LR: Adam, lr = 2e-4
	•	LR scheduler: ReduceLROnPlateau with mode='min', factor=0.5, patience=3 (stepped on validation loss)
	•	Epochs / batch size: 50 epochs, batch_size = 2
	•	Regularization: [dropout, weight decay, early stopping] (not present)
	•	Hardware/runtime: [GPU/CPU, time] (not recorded)

⸻

8. Evaluation Methodology

8.1 Metrics
	•	Notebook computes and prints:
	•	Training loss per epoch
	•	Validation loss per epoch
	•	Segmentation metrics (mIoU / Dice / pixel accuracy): not implemented in the notebook, despite being discussed in markdown notes.

8.2 Validation Strategy
	•	Fixed split: 80/20 on first n=200 images, no k-fold.

8.3 Error Analysis Plan
	•	Qualitative: there is a visualize_sample(...) function that shows:
	•	input image
	•	ground truth mask
	•	predicted mask (argmax over logits)
	•	optional confidence thresholding logic is present as a parameter, but no final analysis summary is included.

⸻

9. Results

9.1 Quantitative Results (table)

Include:
	•	Baseline vs final model(s)
	•	Mean + std if multiple runs/folds
	•	Per-class metrics if relevant


9.2 Qualitative Results (if applicable)
	•	Example predictions: available via visualize_sample()

9.3 Ablations (optional but strong)
	•	Changes introduced in this notebook:
	•	Reintroduced augmentations (flip/rotate/affine/brightness-contrast)
	•	Added LR scheduler (ReduceLROnPlateau)
	•	Impact on metric: [needs logged values]

⸻

10. Discussion
	•	Why the final approach worked (hypothesis + evidence): [ … ]
	•	Trade-offs: accuracy vs speed, complexity vs interpretability: [ … ]
	•	Generalization risk: domain shift, overfitting signs: [ … ]

⸻

11. Deployment (or “Production Considerations”)
	•	Inference pipeline: input → resize/normalize → model → argmax mask → postprocess
	•	Latency/throughput targets: [ … ]
	•	Monitoring: drift, performance, data quality: [ … ]
	•	Retraining plan: triggers and schedule: [ … ]

⸻

12. Team Contribution (who did what)
==

⸻

13. Conclusion & Next Steps
	•	3–5 bullet summary of outcomes: [ … ]
	•	Next steps: add mIoU/per-class IoU evaluation, verify label mapping + ignore handling, define a true test split, report final numbers.

⸻

14. References
	•	Torchvision DeepLabV3 ResNet-50, Albumentations, PyTorch: [add proper citations/links]
	•	Dataset source: [ … ]

⸻

What’s still missing (and not in the notebook)
	•	dataset source (Kaggle/NASA/etc.), total dataset size, class names/id mapping
	•	whether any ignore label exists and its value
	•	final metrics: mIoU + per-class IoU, plus a few prediction visuals
	•	hardware used (GPU type) + training time